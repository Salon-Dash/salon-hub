eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
spring:
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    table: flyway_history_booking_service
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
